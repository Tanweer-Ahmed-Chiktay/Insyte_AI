{
  "duplicates": {
    "filesToRemove": [
      "apiClientBackup.js",
      "emailUtilsCopy.js",
      "calendarService.old.js",
      "websocketHandler_temp.js"
    ],
    "reason": "Duplicates or old versions causing confusion"
  },
  "gmailApi": {
    "currentIssue": "Polling causes rate limits",
    "improvement": "Use Gmail Push Notifications (Pub/Sub) for real-time updates",
    "codeSnippet": "function subscribeToGmailPush() { /* Set up Pub/Sub subscription to Gmail push notifications */ }",
    "explanation": "Instead of polling, subscribe to Gmail push notifications to receive updates instantly and reduce API calls."
  },
  "websocket": {
    "currentIssue": "No websocket server to push updates to clients",
    "improvement": "Implement Node.js websocket server that listens for push notifications and broadcasts updates",
    "codeSnippet": "const WebSocket = require('ws');\nconst wss = new WebSocket.Server({ port: 8080 });\nwss.on('connection', ws => { /* Send updates on notification */ });",
    "explanation": "Frontend connects to this WebSocket for live updates without polling."
  },
  "calendar": {
    "currentIssue": "Partial read-only support, no event add/delete",
    "improvement": "Implement full CRUD for Google Calendar and Microsoft Graph",
    "codeSnippet": "async function addGoogleCalendarEvent(event) {\n  return await calendarAPI.post('/calendars/primary/events', event);\n}",
    "explanation": "Use full API scopes to allow event creation, editing, deletion with proper syncing."
  },
  "emailLabels": {
    "currentIssue": "Labels not fully synced or real-time",
    "improvement": "Use Gmail History API to track label changes, update UI on WebSocket notification",
    "codeSnippet": "async function fetchLabelEmails(labelId, historyId) { /* fetch emails with label, track changes with historyId */ }",
    "explanation": "Keeps tabs (Inbox, Sent, Trash, etc.) always in sync efficiently."
  },
  "slackIntegration": {
    "currentIssue": "No Slack integration present",
    "improvement": "Add Slack tab, use Slack Web API and Events API for real-time updates",
    "codeSnippet": "async function fetchSlackChannels() { /* Use Slack Web API */ }",
    "explanation": "Integrate Slack data alongside emails for unified messaging experience."
  }
}